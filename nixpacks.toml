# nixpacks.toml
#
# ראילוואי בונה את הפרויקט באמצעות Nixpacks, שמזהה אוטומטית שזה
# פרויקט Node.js (בזכות package.json) - אבל הוא לא יודע שאנחנו צריכים
# גם את ffmpeg (כלי חיצוני לעיבוד אודיו, לא ספריית npm) עד שנגיד לו
# את זה במפורש כאן.

[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]
